// src/types/navigation.ts

/**
 * @file This file defines the type mappings for the React Navigation stack.
 * This ensures type safety for screen names and their parameters.
 *
 * @format
 */

// eslint-disable-next-line @typescript-eslint/consistent-type-definitions
export type RootStackParamList = {
  Splash: undefined; // The Splash screen requires no parameters.
  Home: undefined; // The Home screen requires no parameters.
};
